import { Stack } from "expo-router";

export default function LeagueLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: true,
            }}
        />
    );
}
